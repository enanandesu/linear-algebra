package KCN-judu/linear-algebra/internal

// Values

// Types and methods

// Type aliases

// Traits
pub trait Add {
  op_add(Self, Self) -> Self
}
impl Add for Int

pub trait Conjugate {
  conjugate(Self) -> Self
}

pub trait Mul {
  op_mul(Self, Self) -> Self
}
impl Mul for Int

pub trait Neg {
  op_neg(Self) -> Self
}
impl Neg for Int

pub trait One {
  one() -> Self
}
impl One for Int

pub trait Zero {
  zero() -> Self
}
impl Zero for Int

